region = Region.find(params[:id])
[0,1].each { |i| 
  page["region_id_#{params[:random_identifier]}"].up('.address').down('.region_name', i).value  = region.name
  page["region_id_#{params[:random_identifier]}"].up('.address').down('.country_name', i).value = region.country.name  
}
