namespace :osirails do
  namespace :has_search_index do
    desc "Run all unit, functional and integration tests for has_search_index feature"
    task :test do
      puts "!!! You have to run test from the plugin directory by running 'rake test' !!!"
    end
  end
end
