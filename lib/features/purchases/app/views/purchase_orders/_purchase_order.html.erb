<% unless params['supplier_id'] %>
  <div class="presentation_medium">
    <p>
      Vous &ecirc;tes sur la page de cr&eacute;ation d'ordre d'achats.<br/>
      Pour cr&eacute;er un nouvel ordre ordre d'achats, vous devez choisir le fournisseur qui sera associ&eacute; &agrave; cet ordre.
    </p>
    <p>Pour cet ordre, avez-vous affaire &agrave; un fournisseur connu OU &agrave; un nouveau fournisseur ? Si vous ne savez pas, n'h&eacute;sitez pas &agrave; faire une recherche.</p>
    
    <h2>1. Fournisseur existant</h2>
    <% form_for @purchase_order, :url => { :action => :new }, :html => { :method => :get } do |form| -%>
      <div id="purchase_order_search_supplier">
        <%= hidden_field_tag :supplier_id %>
        <%= custom_text_field_with_auto_complete(:supplier, :name, { :value => "Chercher un fournisseur dans la base" }, { :update_id => "supplier_id" } ) %>
        <%= form.submit 'Valider' %>
      </div>
    <% end -%>
    
    <h2>2. Nouveau Fournisseur</h2>
    <% form_tag new_supplier_path, :method => :get do %>
      <p>
        <%= hidden_field_tag :purchase_order, 1 %>
        <%= submit_tag "Cr&eacute;er un nouveau fournisseur" %>
      </p>
    <% end %>
  </div>
<% end %>
