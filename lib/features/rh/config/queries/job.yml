conf_pagination: &pagination
  per_page:
    - 50
    - 100
    - 200
    
advanced_job:
  columns:
    - name
    - service.name
  filters:
    - name
    - service: service.id
  order:
    - id
  <<: *pagination
    
job_index:
  filters:
    - name
    - service: service.id
  quick_search:
    - name
    - service.name
  columns:
    - name
    - service.name
  group:
    - service.name
  order:
    - id
  <<: *pagination
  default_query:
    columns:
    - name
    - service.name
    order:
      - id
    per_page: 50
