conf_pagination: &pagination
  per_page:
    - 25
    - 50
    - 100
    
employee:
  columns:
    - last_name
    - firs_name
    - numbers.number
    - numbers.indicative.*
  group_by:
    - last_name
  sorts:
    - last_name
    - first_name
#  <<: *pagination  
    
employee_index:
  filters:
    - numbers.number
    - role_name: user.roles.name
  columns:
    - fullname
    - numbers.indicative.country.name
  group_by:
    - last_name
  sorts:
    - last_name
    - first_name
#  <<: *pagination   
  default_query:
    criteria: # * , use search_with query hash instead ?
      fullname:
        action: like
        value: john Doe
      numbers.number:
        action: >=
        value: 0
    columns: #?
      - last_name
      - fullname
      - numbers.number
    group_by: #*
      - last_name
    sorts: #*
      - last_name
      - first_name
    <<: *pagination 
    
