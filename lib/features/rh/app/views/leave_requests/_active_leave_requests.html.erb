<h2>Mes demandes en cours</h2>

<% if active_leave_requests.size > 0 %>

  <div>
    <table>
      <tr>
        <th>Date de début</th>
        <th>Date de fin</th>
        <th>Créée le</th>
        <th>Statut</th>
        <th>Actions</th>
      </tr>
      
      <% for leave_request in active_leave_requests %>
        <% if leave_request.employee_id == current_user.employee.id and leave_request.status != 4 %>
          <tr>
            <td><%= leave_request.start_date.strftime("%A %d %B %Y") %></td>
            <td><%= leave_request.end_date.strftime("%A %d %B %Y") %></td>
            <td><%= leave_request.created_at.to_datetime.strftime("%A %d %B %Y à %Hh%M") %></td>
            <td><%= view_status(leave_request) %></td>
            <td>
              <%= leave_request_link(leave_request, :link_text => "") %>
            </td>
          </tr>
        <% end %>
      <% end %>
      
    </table>
  </div>
  
<% else %>

  <div>
    <p>Aucune demande en cours</p>
  </div>

<% end %>
