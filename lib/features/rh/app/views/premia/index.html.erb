<% add_contextual_menu_item :useful_links do %>
  <%= employee_link(@employee, :link_text => "Voir l'employé") %>
  <%= employees_link(:link_text => "Voir tous les employés") %>
<% end %>

<h1>Historique des primes</h1>

<%= render :partial => 'employees/employee_summary', :locals => { :employee => @employee } %>
<div class="presentation-medium">
  <% if @premia.empty? %>
    <p>Aucune prime</p>
  <% else%>
    <table>
      <thead>
        <tr>
          <th>Montant</th>
          <th>Commentaire</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% for premium in @premia%>
          <tr>
            <td><%=h premium.amount %></td>
            <td><%=h premium.remark %></td>
            <td><%=h l(premium.date) unless premium.date.nil? %></td>
          </tr> 
        <% end %>
      </tbody>
    </table>  
  <% end %>
</div>
