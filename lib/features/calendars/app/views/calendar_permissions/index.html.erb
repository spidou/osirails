<% title_context "Administration" %>
<%= title "Configurer les permissions par calendrier" %>

<div>
  <h2>Les calendriers</h2>
  <ul>
    <% for calendar in @calendars %>
      <li>
        <%= link_to calendar.title || calendar.name, edit_calendar_permission_path(calendar) %>
      </li>	
    <% end %>
  </ul>
</div>
