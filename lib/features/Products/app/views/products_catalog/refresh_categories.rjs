unless params[:parameter].to_i == -1
  category = ProductReferenceCategory.find(params[:parameter].to_i)
    @categories = ProductReferenceCategory.find_all_by_product_reference_category_id(category.id)
      page.replace_html("select_#{category.ancestors.size + 1}", :partial => 'show_categories', :object => @categories)
end