page.replace "establishment#{params['establishment_id']}_address_city_name", 
  select("establishment#{params['establishment_id']}_address", "city_name",
  City.find(:all, :conditions => ["country_id = ?", 1]).push(Country.new(:name => "AUTRE")).collect {|a| [ a.name, a.name ] })
page.replace "establishment#{params['establishment_id']}_address_country_name", 
  select("establishment#{params['establishment_id']}_address", "country_name", Country.find(:all).push(Country.new(:name => "AUTRE")).collect {|a| [ a.name, a.name ] })