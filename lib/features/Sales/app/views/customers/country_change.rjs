country = Country.find_by_name("#{params["values"].split}")
page.replace "city_#{params["establishment_id"]}_name", 
  my_text_field_with_auto_complete(:city, :name, {:index => params["establishment_id"], :country_id => country.id, :size => 15})