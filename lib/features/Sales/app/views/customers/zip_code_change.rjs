city = City.find_by_zip_code("#{params["values"].strip}")
page["city_#{params["establishment_id"]}_name"].value = "#{city.name}"
#page["country_#{params["establishment_id"]}_name"].value = "#{Country.find(city.country).name}"
