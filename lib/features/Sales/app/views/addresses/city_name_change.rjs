country = Country.find_by_name(params[:country_name])
unless country.nil?
  city =City.find_by_name(params[:city_name], :conditions => ["country_id = ?", country.id])
  unless city.nil?
    page["city_zip_code"].value = city.zip_code
  end
end