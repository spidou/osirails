cities =City.find_all_by_zip_code(params[:city_zip_code])
if cities.length == 1
  page["new_city#{params[:cpt]}_name"].value = cities[0].name
  country = Country.find_by_id(cities[0].country_id)
  unless country.nil?
    page["new_country#{params[:cpt]}_name"].value = country.name
  end
elsif country != nil
  cities.each do |city|
      if city.country_id = country.id
        page["new_city#{params[:cpt]}_name"].value = city.name
      end
  end
end


