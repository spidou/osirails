country = Country.find_by_name(params[:country_name])
unless country.nil?  
  city =City.find_by_zip_code(params[:city_zip_code], :conditions => ["country_id = ?", country.id])
  unless city.nil?
    page["city_name"].value = city.name
  end
end
