en:
  activerecord:
    models:
      menu: "Menu"
      role: "Role"
      service: "Service"
      user: "User"
    attributes:
      menu:
        parent: "Parent menu"
        separator: "Insert a separator"
      role:
        name: "Role name"
        description: "Role description"
        users: "Members"
      service:
        service_parent: "Parent service"
      user:
        username: "User account name"
        enabled: "Enabled account ?"
        force_password_expiration: "Ask user for a new password on next login"
  integrated_search:
    attributes:
      user:
        roles:
          name: "Roles"
    filters:
        user:
          roles: "Roles"
    page_names:      
      user:
        user_index: "Users"
