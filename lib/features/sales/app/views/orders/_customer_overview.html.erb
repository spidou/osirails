<%
require_locals order
customer = order.customer
%>

<div class='presentation_medium'>
  <h2>Informations concernant le client</h2>
  <% form_for customer do |form| %>
    <% form.force_show_view = true %>
    <%# render :partial => 'thirds/third', :object => order.customer, :locals => { :form => form } %>
    <%= render :partial => 'customers/customer_stats', :object => order.customer %>
  <% end %>
</div>
