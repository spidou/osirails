<%= order_header %>

<% add_contextual_menu_item :possible_actions do %>
  <%= display_add_button(@order) %>
  <%= display_list_button(@step) %>
  <%= display_show_button(@order, @press_proof)%>
  <%= display_dunning_add_button(@order, @press_proof)%>
<% end %>

<%= render :partial => 'press_proof', :object => @press_proof, :locals => {:button_name => "Modifier", :action => :update, :mockups => @mockups} %>
