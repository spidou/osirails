<%= order_header %>

<h1>Modification du bon de livraison</h1>

<div class="presentation_medium">
	<% form_for @delivery_note, :url => { :controller => "delivery_notes", :action => "update", :order_id => @order.id }, :method => :put do |form| %>
	  <%= render :partial => 'delivery_note_form', :object => @delivery_note, :locals => { :form => form } %>
  <% end %>
</div>
