<% add_contextual_menu_item :delivery_note do %>
  <%= display_delivery_note_add_button(@order) unless is_new_view? %>
  <%= display_delivery_note_show_button(@order, @delivery_note) unless is_show_view? %>
  <%= display_delivery_note_download_attachment_button(@order, @delivery_note) %>
  <%= display_delivery_note_show_pdf_button(@order, @delivery_note) %>
  <%= display_delivery_note_edit_button(@order, @delivery_note) unless is_edit_view? %>
  
  <%= display_delivery_note_confirm_button(@order, @delivery_note) %>
  <%= display_delivery_note_realize_button(@order, @delivery_note) %>
  <%= display_delivery_note_schedule_button(@order, @delivery_note) %>
  <%= display_delivery_note_sign_button(@order, @delivery_note) %>
  <%= display_delivery_note_cancel_button(@order, @delivery_note) %>
  <%= display_delivery_note_delete_button(@order, @delivery_note) %>
  <%= display_delivery_note_list_button(@step) %>
<% end %>
