<% quote = quote_in_one_line %>

<tr class="quote">
  <td><%= quote.created_at.to_humanized_date %></td>
  <td><%= quote.number_of_pieces %></td>
  <td><%= quote.total_with_taxes.round_to(2).to_s(2) %></td>
  <td><%= quote.creator.employee_name %></td>
  <td>
    <%= order_step_estimate_quote_link_overrided(@order, quote, :link_text => "", :options => { :format => :pdf }) %>
    <%= edit_order_step_estimate_quote_link_overrided(@order, quote, :link_text => "") %>
    <%= delete_order_step_estimate_quote_link_overrided(@order, quote, :link_text => "") %>
  </td>
</tr>
