conf_filters: &filters
  filters:
conf_columns: &columns
  columns:
    - name
    - legal_form.name
    - website
    - head_office.name
    - head_office.siret_number
    - head_office.full_address
    - head_office.establishment_type.name
    - head_office.activity_sector_reference.code
    - head_office.activity_sector_reference.get_activity_sector_name
    - head_office.phone.formatted
    - head_office.fax.formatted
    - bill_to_address.formatted
    - factor.name_and_fullname
    - customer_grade.name
    - customer_solvency.name
    - brand_names
conf_group: &group
  group:
    - legal_form.name
    - factor.name
    - customer_grade.name
    - customer_solvency.name 
conf_order: &order
  order:
    - name
    - legal_form.name
    - customer_grade.name
    - factor.name
    - customer_solvency.name
conf_pagination: &pagination
  per_page:
    - 50
    - 100
    - 200
    
    
advanced_customer:
  filters:
    - name
    - legal_form.id
    - website
    - head_office.name
    - head_office.siret_number
    - head_office.full_address
    - head_office.establishment_type.id
    - head_office.activity_sector_reference.id
    - head_office.activity_sector_reference.code
    - head_office.activity_sector_reference.get_activity_sector_name
    - head_office_phone: head_office.phone.formatted
    - head_office_fax: head_office.fax.formatted
    - bill_to_address: bill_to_address.formatted
    - factor.id
    - customer_grade.id
    - customer_solvency.id
  <<: *columns
  <<: *group
  <<: *order
  <<: *pagination

customer_index:
  filters:
    - name
    - legal_form.id
    - website
    - head_office_name: head_office.name
    - head_office_siret_number: head_office.siret_number
    - head_office_activity_sector_reference: head_office.activity_sector_reference.id
    - head_office_phone: head_office.phone.formatted
    - head_office_fax: head_office.fax.formatted
    - factor.id
    - customer_grade.id
    - customer_solvency.id
  <<: *columns
  <<: *group
  <<: *order
  <<: *pagination
  quick_search:
    - name
    - legal_form.name
    - head_office.name
    - head_office.siret_number
    - head_office.phone.formatted
    - head_office.fax.formatted
  default_query:
    columns:
      - name
      - legal_form.name
      - brand_names
      - head_office.activity_sector_reference.get_activity_sector_name
      - customer_grade.name
    order:
      - name
    per_page: 50
