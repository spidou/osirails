page.remove("link_add_remove_establishment_#{params[:cpt]}")

page.insert_html(:bottom, "new_establishment", 
  :partial => 'establishments/new_establishment_form',  :locals => {:cpt=> params[:cpt], :error => false})
page["new_establishment_number_value"].value = params[:cpt]