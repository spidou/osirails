<% secondary_menu POSSIBLE_ACTIONS do %>
  <%= show_customer_add_button("Ajouter un client") %>
<% end %>

<h1> Liste des clients </h1>

<div class="frame">
  <% unless @customers.empty?%>
    <table>
      <tr>
        <th>Nom</th> 
        <th>Secteurs d&apos;activités</th>
        <th>Note</th>
        <th>Actions</th>
      </tr>

      <%for customer in @customers%>
        <tr>
          <td><%= customer.name %></td>
          <td><%= customer.activity_sector.name unless  customer.activity_sector.nil?%></td>
          <td><%= customer.note %></td>
          <td>
            <%= show_customer_view_button(customer) %> 
            <%= show_customer_edit_button(customer) %>
            <%= show_customer_delete_button(customer) %>
          </td>
        </tr>
    <%end%>
  <%end%>
  </table>
</div>
