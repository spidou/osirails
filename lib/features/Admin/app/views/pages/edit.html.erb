<h4>Edit a page</h4>

 <% form_for(@page) do |f| %>
   <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f} %><br/><br/>
  <%= f.submit 'Mettre à jour' %>
<% end %> 

<%= link_to 'Retour', :action => 'index' %>