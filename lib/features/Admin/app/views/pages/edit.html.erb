<h4>Edit a page</h4>

 <% form_for(@page) do |f| %>
   <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  
  <%= f.submit 'Edit' %>
<% end %> 

<%= link_to 'Retour au menu', :action => 'index' %>