fr:
  activerecord:
    errors:
      messages:
        invalid_date: "n'est pas une date valide"
        invalid_time: "n'est pas une heure valide"
        invalid_datetime: "n'est pas une date valide"
        equal_to: "doit être le {{restriction}}"
        before: "doit être avant le {{restriction}}"
        on_or_before: "doit être le ou avant le {{restriction}}"
        after: "doit être après le {{restriction}}"
        on_or_after: "doit être le ou après le {{restriction}}"
        between: "doit être entre le {{earliest}} et le {{latest}}"
  validates_timeliness:
    error_value_formats:
      date: '%d/%m/%Y'
      time: '%H:%M:%S'
      datetime: '%d/%m/%Y à %H:%M:%S'
