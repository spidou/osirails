Benjamin et Julien :

GET users/1
 NoMethodError in Users#show 
You have a nil object when you didn't expect it!
The error occurred while evaluating nil.strftime
>> L'erreur survient lorsqu'on veut voir les détails sur un utilisateur qui ne s'est jamais connecté (dernière connexion = nil)

Admin/app/views/users/edit.html.erb :
Role.find(:all, :order => :name) > utiliser une variable de classe @roles que l'on instancie dans le controlleur

Admin/app/views/roles/_role.html.erb :
User.find(:all, :order => :name > meme chose que pour les roles




Ulrich et Jérémie :
up/down/top/bottom dans une autre colonne du tableau (colonne POSITION)
gérer l'indentation au niveau du CSS > afficher <img class='admin_pages_indent' alt='___' title='identation'/>
                                             ou <span class='admin_pages_indent'/> # dans le css : .admin_pages_indent { width : 10px }
afficher -- Racine -- pour le premier élément du select box pour le formulaire d'ajout et d'édition
françiser les liens : up, down, edit, delete, add a page, home
          les boutons : add, edit
          les textes : title link, description link


pages_controller.rb :
supprimer méthode list, utiliser la méthode index et renommer la vue list.html.rb en index.html.erb
rajouter des commentaires pour chaque méthode (voir classe user.rb)
list + new + edit : ajouter un commentaire pour dire a quoi sert la variable @pages
françiser les messages dans les flash
possibitité de se passer de move_up et move_down ou pas ????

page.rb :
supprimer la méthode add_list_item (a quoi sert-elle au fait? elle n'est plus utilisée on dirait)
renommer les méthodes move_up? et move_down?
renommer la méthode get_pages_array (get_structured_pages)
renommer la méthode insert_page (voir peut être la placer dans la méthode get_structured_pages ?)
commenter toutes les méthodes (par exemple position_in_bounds)

pages_helper.rb
faire 2 méthodes : show_up_button, show_down_button au lieu de show_move_buttons